import { IonHeader, IonToolbar, IonTitle } from "@ionic/react";



const Header: React.FC = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>Zulu App</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Header;